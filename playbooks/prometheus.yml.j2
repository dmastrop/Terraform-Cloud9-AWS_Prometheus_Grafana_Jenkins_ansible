global:
  scrape_interval:     15s # By default, scrape targets every 15 seconds.
scrape_configs:
  - job_name: 'node'
    static_configs:
      #{- targets: ['localhost:9100']}#
      -targets: ['{{ listen_address }}:{{ listen_port }}']
      #{ prometheus instructed to listen to targest runnning on this address and port}#
      #{ NOTE that ping 0.0.0.0 on ubuntu will go to localhost }#